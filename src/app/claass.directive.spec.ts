import { ClaassDirective } from './claass.directive';

describe('ClaassDirective', () => {
  it('should create an instance', () => {
    const directive = new ClaassDirective();
    expect(directive).toBeTruthy();
  });
});
